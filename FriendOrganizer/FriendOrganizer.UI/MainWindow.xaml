<Window x:Class="FriendOrganizer.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FriendOrganizer.UI"
        xmlns:view="clr-namespace:FriendOrganizer.UI.View"
        mc:Ignorable="d"
        Title="FriendOrganizer" Height="350" Width="525" FontSize="20">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200" />
            <ColumnDefinition Width="*"/> <!--If not specify, take the left over space.-->
        </Grid.ColumnDefinitions>
        <view:NavigationView DataContext="{Binding NavigationViewModel}"/>
        <!--<ListView ItemsSource="{Binding Friends}"
                  SelectedItem="{Binding SelectedFriend, Mode=TwoWay}" 
                  DisplayMemberPath="FirstName"  />-->

            <!--Binding the GUI selected item to selectedFriend-->
        <!--  Mode=TwoWay is also written back into the ViewModel-->
        <!--DisplayMemberPath set the value want to be displayed-->
        <view:FriendDetailView Grid.Column="1" DataContext="{Binding FriendDetailViewModel}"/>
        
    </Grid>
</Window>
